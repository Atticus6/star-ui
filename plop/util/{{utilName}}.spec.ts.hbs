/* eslint-disable no-console */
import { describe, expect, it } from 'vitest'
import { {{utilName}} } from ".";

describe('test {{utilName}}', () => {
  it('test {{utilName}} 1+2=3', () => {
    const res = {{utilName}}(1, 2)
    console.log(res)

    expect(res).toBe(3)
  })
})
